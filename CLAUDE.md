# Claude Code Instructions

- For web search and current-events lookup, use the `brave-search` MCP tools first.
- Treat `brave-search` as the default search source unless the task explicitly requires a different source.
- Include source links in answers that rely on web search results.
